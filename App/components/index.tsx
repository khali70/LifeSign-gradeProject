// import {useSelector} from 'react-redux';
// import store from '@Redux/store';

// type RootState = ReturnType<typeof store.getState>;

// export const useMainState = (cb: (state: RootState) => any) =>
//   useSelector<RootState, any>(cb);
